events {}

http {
    upstream app {
        server distributed-rate-limiter-app-1:8080;
        server distributed-rate-limiter-app-2:8080;
        server distributed-rate-limiter-app-3:8080;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://app;
        }
    }
}